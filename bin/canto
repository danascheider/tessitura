#!/usr/bin/env ruby

require 'rack'

begin
  system 'thin start -R config.ru'
rescue LoadError => e
  require 'rubygems'
  path = File.expand_path('../../')
  $:.unshift(path) if File.directory?(path) && !$:.include?(path)
  system 'thin start -R config.ru'
end
