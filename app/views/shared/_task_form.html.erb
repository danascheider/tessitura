<%= form_tag(@task, method: @method, remote: true) do %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% @task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag 'title', 'Title: ' %>
    <%= text_field_tag 'task[title]', @task.title %><br>
    <br />
    <%= label_tag 'status', 'Complete: ' %>
    <%= check_box_tag 'task[complete]', '0', false %><br>
    <br />
  </div>
  <div class="actions">
    <%= button_to @button_value, @destination, remote: true %>
  </div>
<% end %>
